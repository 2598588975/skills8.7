interface:
  display_name: "?????"
  short_description: "???????????????????"
  default_prompt: "Use $color-card-prompt to create a production-ready visual prompt."
policy:
  allow_implicit_invocation: true
